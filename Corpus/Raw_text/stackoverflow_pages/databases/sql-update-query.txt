*** sql-update-query ***

 
 
 
 
 
 This question already has answers here :
                            
                         
 
 
 
 How do I UPDATE from a SELECT in SQL Server? 
 
                                    (33 answers)
                                 
 
 Closed  6 years ago . 
 
 
 I have a table   which contains three columns:    
 There is another table   which contains 2 columns  .   column of   is same as of  .  
 I need a query which will update   column of   with   of  . 
 I can do it through a loop by checking ID and executing update statement. How can it has to be done with a query? 
 
 How about 
 
 
 
 
 
 