*** what-is-a-scalar-query ***

 I am using LLBLGEN where there is a method to execute a query as a  . Googling gives me a definition for  , are they the same ? 
 
 A scalar query is a query that returns one row consisting of one column. 
 
 For what it's worth: 
 
 Scalar subqueries or scalar queries are queries that return exactly
  one column and one or zero records. 
 
 Source 
 
 A scalar method is used for aggregate functions such as max, min, sum, avg and so on. Whenever you want to return a single value after execute the query. 
 